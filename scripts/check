#!/bin/bash
set -e

PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT:-$(dirname "$(dirname "$0")")}"
BIN="${PLUGIN_ROOT}/bin/cc-allow"

# Install if binary doesn't exist
if [ ! -x "$BIN" ]; then
  "${PLUGIN_ROOT}/scripts/install" >&2
fi

exec "$BIN" --hook
