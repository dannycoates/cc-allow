# Default ruleset - minimal rules, mostly askes through to Claude Code

[policy]
default = "ask"
dynamic_commands = "ask"

[commands.deny]
names = ["sudo", "su"]
message = "Privilege escalation not allowed"

[constructs]
function_definitions = "ask"
background = "ask"
subshells = "ask"
