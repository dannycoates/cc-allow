# Default ruleset - minimal rules, mostly asks through to Claude Code

version = "1.0"

[policy]
default = "ask"
dynamic_commands = "ask"

# ============================================================================
# DENIED COMMANDS
# ============================================================================

[deny]
commands = ["sudo", "su"]
message = "Privilege escalation not allowed"

# ============================================================================
# SHELL CONSTRUCTS
# ============================================================================

[constructs]
function_definitions = "ask"
background = "ask"
subshells = "ask"
